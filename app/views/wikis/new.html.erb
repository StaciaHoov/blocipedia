<h1>New Wiki</h1>
<div class='row'>
    <div class='col-md-4'>
        <p> Guidelines for Wikis </p>
        <ul>
            <li> Make it informative </li>
            <li> Make it fair </li>
        </ul>
    </div>
    <div class='col-md-8'>
        <%= form_for @wiki do |f| %>
            <div class="form-group">
                <%= f.label :title %>
                <%= f.text_field :title, class: 'form-control', placeholder: "Enter title" %>
            </div>
            <div class="form-group">
                <%= f.label :body %>
                <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter text" %>
            </div>
            <div class="form-group">
                <%= f.submit "Save", class: 'btn btn-success' %>
            </div>
        <% end %><br>
        
        <% if current_user.role == 'standard' %>
            <%= form_tag charges_path do %>
                <h4> Create private Wikis and other options by upgrading to a <em>Premium</em> account. </h4>
                <script class='stripe-button' src="https://checkout.stripe.com/checkout.js" data-key="<%= @stripe_btn_data[:key] %>"
                    data-amount="<%= @stripe_btn_data[:amount] %>" data-description="<%= @stripe_btn_data[:description] %>"> 
                </script>
            <% end %>
        <% end %>
        
    </div>
</div>